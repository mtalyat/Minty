#pragma once

#include "M_Main.h"

namespace minty
{
	/// <summary>
	/// A tag component that marks an entity so it will not be destroyed between Scenes, unless this tag is removed.
	/// </summary>
	struct MINTY_API NoDestroy
	{

	};
}
# Syntax

PascalCase for structs and classes
PascalCase for functions
camelCase for variables
const on everything that can be const
const keyword comes after the thing it applies to
public facing elements must have javadoc style /** @brief */ comments